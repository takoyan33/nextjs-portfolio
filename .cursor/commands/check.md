---
description: 動作確認のコマンド
---

1. `git diff` 等で変更内容の差分を確認する
2. `nextjs_index` ツールでサーバーが起動しているか確認する。もしポート3000番台でサーバーが見つからなければ起動する
3. Playwright MCPを用いて、変更に関連するページをブラウザで開く。PCサイズ(1280x720)とSPサイズ(375x812, iPhone X等)の両方でスクリーンショットを撮り、UI崩れやコンソールエラーがないか確認する
4. 問題なければレポートして終了
